import { TypeStatusOrderPipe } from './type-status-order.pipe';

describe('TypeStatusOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeStatusOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
