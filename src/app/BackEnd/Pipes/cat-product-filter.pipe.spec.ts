import { CatProductFilterPipe } from './cat-product-filter.pipe';

describe('CatProductFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CatProductFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
